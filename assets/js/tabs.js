// JavaScript Document

$(document).ready(function() {
	$('.tablist').on('click', '.tab-button', function() {
		// get the data name
		// match data name with ID of panel
		// see if panel is visible
		// if visible, do nothing
		// if not visible, hide other panels, show THIS pane
		//
		// var panel = $(this).attr('data-panel');
		// $('.productCategory').removeClass('active');
		// $('#' + panel).addClass('active');
		
	});
});